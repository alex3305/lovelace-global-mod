default:
  mods:
    '/':
      - selector: 'ha-drawer'
        style: |
          ha-sidebar { background-color: yellow; }
    livingroom:
      - selector: 'partial-panel-resolver ha-panel-lovelace$hui-root div'
        style: |
          header: { background-color: red; }
